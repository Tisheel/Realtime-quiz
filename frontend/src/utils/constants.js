export const WS_ROOM = `wss://${window.location.host}/room`
export const WS_TEACHER = `wss://${window.location.host}/teacher`
export const GET_TESTS = `https://${window.location.host}/v1/test`
export const LOGIN = `https://${window.location.host}/v1/login`