export const WS_ROOM = `ws://${window.location.host}/room`
export const WS_TEACHER = `ws://${window.location.host}/teacher`
export const GET_TESTS = `http://${window.location.host}/v1/test`
export const LOGIN = `http://${window.location.host}/v1/login`